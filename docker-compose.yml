services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: techup-api
    env_file:
      - .env
    ports:
      - "${PORT}:3000"
    restart: unless-stopped
    command: ["./server"]

volumes:
  db_data: